incializa GIT en una carpeta:
        git init
poner usuario
        git config --name "nombre"
poner EMAIL
        git config --email "correo"
revisar historial de commit
        git log

ESTO GENERARÁ UN CONFLICTO :( )

